void setup() {
  ?.?(?); //시리얼 통신을 9600의 속도로 시작한다  
}

void loop() {
  ? ? = ?(?); // int라는 크기를 가진 val방에 A0의 아날로그 입력값을 저장한다.
  Serial.println(?); // val이라는 값을 시리얼 모니터에 출력해준다.
  delay(?); // 0.3초 기다린다. 
}
