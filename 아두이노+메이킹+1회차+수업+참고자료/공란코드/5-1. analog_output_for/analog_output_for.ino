void setup() {
  pinMode (9,OUTPUT);//LED와 연결한 핀(9)을 출력으로 설정한다.
}

void loop() {
  for(int i = 0 ; ? ; ?)
  {// 초기값: i에 0저장 ; 조건값: i가 255보다 작거나 같다면 ;증가값: i를 5씩 증가
    ?(9,?); //LED를 연결한 핀을 i의 값으로 출력한다.
    delay(300); // 0.3초 기다린다.
  }  
}
